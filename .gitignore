# Dependencies
node_modules/

# Build artifacts — compiled server output (not needed, Bun runs TS directly)
dist/*.js
dist/*.d.ts
dist/*.js.map
dist/*.d.ts.map
dist/commands/
dist/utils/
# dist/frontend/ is intentionally NOT ignored — committed so `bun add -g` works out of the box

public/dist/

# Environment files
.env

# Logs
*.log

# Editor artifacts
.cursor/
.claude/

# Development artifacts
*.excalidraw

docs/